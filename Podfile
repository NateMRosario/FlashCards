# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlashcardsWithFirebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FlashcardsWithFirebase
  pod 'Firebase/Core'
  pod ‘Firebase/Database’
  pod ‘Firebase/Auth’
  pod ‘SnapKit’

  target 'FlashcardsWithFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlashcardsWithFirebaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
